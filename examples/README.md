# Examples

These are sanitized, realistic inputs the CLI consumes for a deterministic demo.

- `iac/`: desired vs current inventory snapshots (multi-cloud + Kubernetes)
- `cicd/`: pipeline policy surface (reliability + release safety)
- `incidents/`: incident timeline + readiness signals
